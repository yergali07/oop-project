package edu.kbtu.university.enums;

/**
 * Priority of a news item — {@code CRITICAL} should be surfaced
 * immediately to every subscriber.
 */
public enum UrgencyLevel {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}