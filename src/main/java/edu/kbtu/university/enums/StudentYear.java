package edu.kbtu.university.enums;

/**
 * Bachelor-program year (1..4). Masters and PhD students live outside
 * this enum and are tagged via the {@code MP-} / {@code PHD-} id prefix.
 */
public enum StudentYear {
    FIRST,
    SECOND,
    THIRD,
    FOURTH
}