package edu.kbtu.university.enums;

/**
 * Role of a {@link edu.kbtu.university.users.User} — drives menu
 * dispatch, reporting, and access control.
 */
public enum Role {
    STUDENT,
    TEACHER,
    MANAGER,
    ADMIN,
    RESEARCH_EMPLOYEE
}